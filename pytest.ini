[pytest]
# 1. Console Logging (CLI)
log_cli = true
log_level = INFO

# 2. File Logging (Saves to a folder named 'logs')
log_file = logs/automation_run.log
log_file_level = INFO
log_file_format = %(asctime)s: %(levelname)s: %(message)s
log_file_date_format = %m/%d/%Y %I:%M:%S %p